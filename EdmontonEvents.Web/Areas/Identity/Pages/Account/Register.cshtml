@page
@model RegisterModel

@{
    ViewData["Title"] = "Sign up";
}

@section styles {
    <link rel="stylesheet" type="text/css" href="~/css/auth.css" asp-append-version="true" />
}


<div class="auth-page">
    <div class="container px-3">
        <div class="auth-card row g-0 overflow-hidden rounded-4">
            <div class="col-lg-6 order-lg-1 order-2">
                <div class="auth-card-form">
                    <p class="auth-eyebrow mb-0">Welcome</p>
                    <h1 class="auth-title">Sign up</h1>
                    <p class="auth-subtitle">Create an account to sign up for events and manage your listings.</p>

                    <form id="account" method="post" novalidate>
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2" role="alert"></div>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-floating mb-3">
                                    <input asp-for="Input.FirstName"
                                           class="form-control form-control-lg"
                                           autocomplete="given-name"
                                           aria-required="true" />
                                    <label asp-for="Input.FirstName">First Name</label>
                                    <span asp-validation-for="Input.FirstName" class="text-danger small"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating mb-3">
                                    <input asp-for="Input.LastName"
                                           class="form-control form-control-lg"
                                           autocomplete="family-name"
                                           aria-required="true" />
                                    <label asp-for="Input.LastName">Last Name</label>
                                    <span asp-validation-for="Input.LastName" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Input.Email"
                                   class="form-control form-control-lg"
                                   autocomplete="username"
                                   aria-required="true"
                                   placeholder="name@example.com" />
                            <label asp-for="Input.Email">Email</label>
                            <span asp-validation-for="Input.Email" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-4">
                            <input asp-for="Input.Password"
                                   class="form-control form-control-lg"
                                   autocomplete="current-password"
                                   aria-required="true"
                                   placeholder="Password" />
                            <label asp-for="Input.Password">Password</label>
                            <span asp-validation-for="Input.Password" class="text-danger small"></span>
                        </div>

                        <button id="login-submit" type="submit" class="btn btn-brand btn-lg w-100">
                            Sign up
                        </button>

                        <div class="auth-links d-flex flex-column flex-sm-row flex-wrap gap-2 gap-sm-4">
                            <a asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Already have an account? Log in!</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="col-lg-6 order-lg-2 order-1 auth-card-visual">
                <img src="~/imgs/signup.jpg"
                     alt="Alberta Legislature grounds in Edmonton"
                     class="auth-card-image" />
                <div class="auth-card-visual-overlay" aria-hidden="true"></div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
